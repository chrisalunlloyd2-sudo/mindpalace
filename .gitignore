screenshots/
*.png
target/
readtext.py
patches/applied.json
playtest*.log

# model chat logs -> private repo (not public)
chat_logs/

# installer build artifacts (large binaries — upload to GitHub release instead)
installer/
game_console.log
